import Foundation

public class {{{className}}} {

    // MARK: - Static Properties
    static let bundle: Bundle = Bundle(for: {{className}}.self)
    static var LocalizedStringSourceId: String { return NSLocalizedString("__localizedStringSourceId__", bundle: self.bundle, comment: "The git hash of the content that generated this file") }

    // MARK: - Localized String Properties
{{#each strings}}
    {{#maybeComment value}}Base Value: "{{value}}"{{/maybeComment}}{{#if isTemplate}}    public class func {{key}}(_ args: String...) -> String { return String(format: NSLocalizedString("{{key}}", bundle: self.bundle, comment: ""), arguments: args) }
{{else}}    public static var {{key}}: String { return NSLocalizedString("{{key}}", bundle: self.bundle, comment: "") }
{{/if}}
{{/each}}
}
